import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "تسویه حساب | پرودید",
};

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return children;
}
